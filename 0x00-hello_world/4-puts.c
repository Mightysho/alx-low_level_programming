#include <stdio.h>
/**
 * main - Entry point
 *
 * puts - To write a new line or string to the output. It prints
 * the passed string with a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
